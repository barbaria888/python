L = float(input('enter the length of floor'))
B = float(input('enter the breadth of floor'))
l = float(input('enter the length of tile'))
b = float(input('enter the length of tile'))

n = int( (L * B) / (l * b) 5) 
print('the number of tiles required are ',n)